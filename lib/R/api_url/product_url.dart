import 'package:shamo/R/api_url/base_url.dart';

abstract class ProductURL {
  // Product
  static const String category = '${baseURL}categories';
  static const String products = '${baseURL}products';
}
